# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Polly Weather' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Polly Weather
  pod 'Alamofire', '~> 5.2'
  pod 'Kingfisher', '~> 6.3'
  pod 'SkeletonView'

  target 'Polly WeatherTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Polly WeatherUITests' do
    # Pods for testing
  end

end
